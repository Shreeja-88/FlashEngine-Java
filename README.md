# FlashEngine-Java
A high-throughput, thread-safe in-memory flash sale simulator built with Core Java. Demonstrates lock-free concurrency (Atomic CAS), thread pools (ExecutorService), concurrent collections, and post-sale analytics using the Stream API.
